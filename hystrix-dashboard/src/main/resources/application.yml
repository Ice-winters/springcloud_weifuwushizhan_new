server:
  port: 9900
spring:
  application:
    name: hystrix-dashboard
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/
    register-with-eureka: false
    fetch-registry: true